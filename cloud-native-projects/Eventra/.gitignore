# Compiled binaries
# NOTE: anchored to the repo root so it does NOT ignore the cmd/server/ source
# directories (that's why /server, not server).
/server
*.exe

# Environment files
.env
*.env
.env.local

# IDE / editor
.DS_Store
.idea/
.vscode/
